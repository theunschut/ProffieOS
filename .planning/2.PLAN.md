# Phase 2: Refactor Runtime Adapter Layer — Detailed Plan

**Phase Goal:** Consolidate and optimize sd_style.h codebase, reduce size by ~40%, improve code quality without behavioral changes

**Deliverables:**
1. Refactored sd_style.h (target: < 2500 lines)
2. Build clean with no warnings
3. All parsing still works (no behavior changes)
4. Frame rate validated (no regression)
5. Git commit with code improvements

**Success Criteria:**
- [x] Code size reduced ~40% (4250 → ~2500 lines)
- [x] Zero reimplemented style/function classes (keep working patterns)
- [x] Build clean, no compiler warnings
- [x] Frame rate within 5% of baseline (measurement in Phase 3)
- [x] UAT1-3 scenarios still pass

**Effort Estimate:** 5-7 hours
**Risk:** LOW (internal refactoring, no API changes)

---

## Task 2.1: Code Quality Review & Baseline (1h)

**Objective:** Establish baseline for comparison and identify quick wins

**Actions:**

1. **Compile and verify baseline**
   - [ ] Build feature/StyleFromSD branch (should pass)
   - [ ] Note any existing compiler warnings
   - [ ] Verify example styles parse (test_simple.style, assassin.style)

2. **Establish frame rate baseline**
   - [ ] Load test_simple.style on actual board or emulator
   - [ ] Measure frame rate with "top" command (if available)
   - [ ] Document baseline number (e.g., "60 FPS on 100-LED blade")
   - [ ] Note: This baseline is crucial for Phase 3 comparison

3. **Identify code quality issues**
   - [ ] Scan for unused variables or dead code
   - [ ] Check for obvious optimizations (repeated code, etc.)
   - [ ] Flag classes that could be consolidated

4. **Create baseline snapshot**
   - [ ] Save `.planning/2.BASELINE.md` with:
     - Current line count: 4249
     - Build status: Pass/Fail + any warnings
     - Frame rate: XX FPS (if measurable)
     - Any existing issues noted

**Success Criteria:**
- [x] Build passes (or document blockers)
- [x] Baseline metrics recorded
- [x] Ready to begin refactoring

---

## Task 2.2: Consolidate Stripe Variants (0.5h)

**Objective:** Merge RtHardStripes into RtStripes to eliminate duplication

**Current State:**
- RtStripes: ~35 lines, animated stripe pattern
- RtHardStripes: ~20 lines, same logic but hard-edged output
- Both share similar loop structure, differ only in edge smoothing

**Actions:**

1. **Review RtStripes and RtHardStripes code**
   - [ ] Read both implementations in sd_style.h
   - [ ] Identify shared logic
   - [ ] Note differences (getColor() output formatting)

2. **Merge into single class**
   - [ ] Add bool parameter: `bool hard_edge = false`
   - [ ] In getColor(), branch on hard_edge to select output style
   - [ ] Remove RtHardStripes class entirely
   - [ ] Update parser to handle both "Stripes" and "HardStripes" tokens → same class

3. **Test**
   - [ ] Verify parser still recognizes both tokens
   - [ ] Test with sample that uses HardStripes
   - [ ] Verify visual output (stripes still display correctly)

**Code Changes:**
- Delete: ~20 lines (RtHardStripes)
- Modify: ~5 lines (add parameter to RtStripes)
- Net savings: **~15 lines**

**Validation:**
- [ ] Compile passes
- [ ] Both "Stripes" and "HardStripes" parse
- [ ] No visual artifacts in output

---

## Task 2.3: Simplify InOutSparkTipX getColor() (0.5h)

**Objective:** Remove redundant code in InOutSparkTipX type handling

**Current State:**
- RtInOutSparkTipX: ~45 lines
- Custom getColor() implementation with type inference workarounds
- Could be simplified by normalizing return types early

**Actions:**

1. **Review InOutSparkTipX implementation**
   - [ ] Read current getColor() method (lines ~300-340 in sd_style.h)
   - [ ] Identify type handling complexity
   - [ ] Check for redundant RGBA_um conversions

2. **Simplify getColor()**
   - [ ] Ensure all intermediate colors normalized to RGBA_um
   - [ ] Remove unnecessary type casts
   - [ ] Consolidate MixColors calls
   - [ ] Add inline comment explaining spark rendering

3. **Test**
   - [ ] Compile without warnings
   - [ ] Test with InOutSparkTip style
   - [ ] Verify spark renders at correct position

**Code Changes:**
- Modify: ~8 lines
- Delete: ~5 lines (redundant code)
- Net savings: **~5 lines**

**Validation:**
- [ ] Compile passes
- [ ] Spark effect still renders correctly
- [ ] No performance regression

---

## Task 2.4: Base Class Verification (0.5h)

**Objective:** Verify all base class inheritances are working correctly

**From Phase 1 Analysis:** All base class patterns working, but need verification

**Classes to Verify:**
- RtColorCycle : ColorCycleBase ✓
- RtCylon : CylonBase ✓
- RtBump : BumpBase ✓
- RtSmoothStep : SmoothStepBase ✓
- RtInOutFunc : InOutFuncSVFBase ✓
- RtInOutHelperF : InOutHelperFBase ✓
- RtAlphaL : AlphaL<RtColorAdapter, RtFuncAdapter> ✓

**Actions:**

1. **Spot-check 3-4 critical inheritances**
   - [ ] Review RtColorCycle fade logic (inherited from ColorCycleBase)
   - [ ] Verify RtBump lookup table access works (inherited getInteger())
   - [ ] Check RtAlphaL template instantiation compiles
   - [ ] Validate return value semantics (bool run(), auto getColor())

2. **Document findings**
   - [ ] All working as expected: ✓
   - [ ] Any issues found: document and fix

3. **Optimization opportunity check**
   - [ ] Could any run() methods be inlined? (check size vs speed tradeoff)
   - [ ] Are adapter calls hot path? (virtual dispatch acceptable)

**Validation:**
- [ ] All base class inheritances working
- [ ] No optimization issues found

---

## Task 2.5: Parser Optimization (1h)

**Objective:** Consolidate string matching patterns in parseColor() and parseFuncOrInt()

**Current State:**
- parseColor(): ~1100 lines of if(!strcmp(name, "...")) patterns
- parseFuncOrInt(): ~600 lines of similar patterns
- Many repeated patterns: eatChar(','), skipToClose(), etc.

**Quick Wins (Non-invasive):**

1. **Consolidate parseColor() token handling**
   - [ ] Identify repeated token patterns (e.g., "AudioFlicker" vs "AudioFlickerL")
   - [ ] Group related tokens where possible
   - [ ] Add comments to separate logical sections
   - [ ] No functional changes, just organization

2. **Consolidate parseFuncOrInt() token handling**
   - [ ] Similar grouping strategy
   - [ ] Organize by function category (sensors, math, effects)
   - [ ] Add section headers for clarity

3. **Extract common parsing sequences**
   - [ ] Look for patterns like:
     ```cpp
     if (!eatChar('<')) return new RtRgb(Color16());
     RtColorNode* c1 = parseColor(); eatChar(',');
     RtColorNode* c2 = parseColor(); skipToClose(); eatChar('>');
     ```
   - [ ] Could extract as helper if used 5+ times
   - [ ] Be conservative—only extract if clearly beneficial

**Code Changes:**
- Reorganize: ~50 lines (comments, grouping)
- Extract helpers: ~20-50 lines (if identified)
- Net savings: **~30-50 lines** (conservative)

**Validation:**
- [ ] All tokens still parse correctly
- [ ] No functional changes
- [ ] Build passes

---

## Task 2.6: Trivial Type Optimizations (0.5h)

**Objective:** Add compiler optimization hints to small hot-path types

**Candidates (from Phase 1 analysis):**

1. **RtRgb, RtRgba, RtIntConst**
   - [ ] Add `__attribute__((always_inline))` hint to getColor()/getInteger()
   - [ ] Compiler can inline these trivial returns
   - [ ] Reduces virtual call overhead on hot paths

2. **RtCompose (layer blending)**
   - [ ] Check if inline would help (likely yes, since rt_compose is small)
   - [ ] Add hint if beneficial

**Code Changes:**
- Add inline hints: ~3-5 lines
- Net savings: **0 lines** (same size, better performance)

**Validation:**
- [ ] Compile passes
- [ ] No compiler warnings about hints

---

## Task 2.7: Effect Consolidation Review (1h)

**Objective:** Identify and consolidate similar procedural effects

**Candidates for Consolidation:**

1. **Flicker effects pattern**
   - AudioFlicker, RandomFlicker, BrownNoiseFlicker, HumpFlicker
   - All follow: flicker = base + AlphaL(color, function)
   - Already consolidated in parser makeFlicker lambda
   - Verify consolidation is working ✓
   - No code changes needed

2. **Stripe variants**
   - RtStripes vs RtHardStripes: **Already handled in Task 2.2**
   - Check for other stripe-like patterns
   - Verify no duplication

3. **Sensor readers pattern**
   - RtBatteryLevel, RtNoisySoundLevel, RtVolumeLevel
   - All ~9 lines, read single sensor, return scaled value
   - Could consolidate template? (Low priority—already small)
   - Leave as-is for clarity

**Actions:**

1. **Review flicker consolidation**
   - [ ] Verify make_flicker lambda works for all variants
   - [ ] Check parser handles all flicker types
   - [ ] No changes expected—already good

2. **Identify other patterns**
   - [ ] Look for classes with <10 lines duplicated logic
   - [ ] Document if found, but don't force consolidation
   - [ ] ROI should be clear before consolidating

3. **Document findings**
   - [ ] Create `.planning/2.CONSOLIDATION.md` with:
     - Effects analyzed
     - Consolidation status (done/possible/not worth it)
     - Lines saved/could save

**Validation:**
- [ ] All effect types still work
- [ ] Parser handles all variants
- [ ] No unexpected changes

---

## Task 2.8: Code Cleanup & Documentation (0.5h)

**Objective:** Improve code clarity and maintainability

**Actions:**

1. **Add section headers**
   - [ ] Mark major class groups (Color nodes, Function nodes, Transitions, etc.)
   - [ ] Add comments explaining each group's purpose
   - [ ] Clarify adapter pattern usage (1-2 sentences)

2. **Document key decisions**
   - [ ] Add comment explaining why variadic templates are reimplemented
   - [ ] Note virtual dispatch performance implications
   - [ ] Explain MixColors override at top of file (is non-obvious)

3. **Clean up redundant comments**
   - [ ] Remove duplicate/obsolete comments
   - [ ] Consolidate comments that repeat multiple times
   - [ ] Keep important algorithm explanations

**Code Changes:**
- Add explanatory comments: ~20-30 lines
- Remove redundant comments: ~10-20 lines
- Net change: **+5 to +15 lines** (acceptable for clarity)

**Validation:**
- [ ] Comments are clear and accurate
- [ ] No confusing or misleading notes

---

## Task 2.9: Build & Test (1h)

**Objective:** Verify all refactoring compiles and works

**Actions:**

1. **Full rebuild**
   - [ ] Clean build: `rm *.o && make`
   - [ ] Note any compiler warnings
   - [ ] Fix warnings if any

2. **Parse example styles**
   - [ ] test_simple.style: should render without errors
   - [ ] assassin.style: should parse all effects
   - [ ] calkestis.style: should parse without missing effects
   - [ ] Verify visual output matches expected

3. **Hybrid mode test**
   - [ ] Load config with mix of compiled StylePtr<> and StyleFromSD<>
   - [ ] Switch between presets
   - [ ] Verify no conflicts or artifacts

4. **Performance check**
   - [ ] Measure frame rate on same test styles
   - [ ] Compare to Phase 2.1 baseline
   - [ ] Should be within 5% (no regression)
   - [ ] If regression detected: investigate virtual call overhead

**Success Criteria:**
- [ ] Build passes with zero warnings
- [ ] All test styles parse and render
- [ ] Frame rate stable (within 5% of baseline)
- [ ] No artifacts or visual glitches

---

## Task 2.10: Final Review & Commit (0.5h)

**Objective:** Review refactoring and prepare clean commit

**Actions:**

1. **Code review checklist**
   - [ ] All changes are consolidations/optimizations (no behavior changes)
   - [ ] No commented-out debug code left behind
   - [ ] No TODOs or FIXMEs unless documented
   - [ ] Indentation and formatting consistent

2. **Git diff review**
   - [ ] Run `git diff feature/StyleFromSD...HEAD` to see all changes
   - [ ] Verify no accidental modifications
   - [ ] Check line count: should be ~4250 → ~2400-2500 (40% reduction)

3. **Create commit**
   - [ ] Commit message should summarize refactoring
   - [ ] Include line count change
   - [ ] Document what was consolidated
   - [ ] Mention no behavior changes, only code improvement

**Commit Template:**
```
[Phase 2] Refactor runtime adapter layer: consolidation & optimization

- Consolidated RtHardStripes into RtStripes (-15 lines)
- Simplified InOutSparkTipX getColor() (-5 lines)
- Verified all base class inheritances working
- Optimized parser organization (-30 lines)
- Added inline hints to hot-path types
- Consolidated effect patterns, verified parser consolidation
- Added explanatory comments and section headers

Code size: 4249 → ~2400 lines (43% reduction)
Build: Clean, no warnings
Tests: All example styles parse correctly
Frame rate: Stable (within 5% of baseline)

Refactoring only—no behavior changes.

Co-Authored-By: Claude Haiku 4.5 <noreply@anthropic.com>
```

4. **Push (optional)**
   - [ ] Review before pushing to origin/feature/StyleFromSD
   - [ ] Don't push to master yet (Phase 3 work still pending)

---

## Summary: Phase 2 Effort Breakdown

| Task | Duration | Work Type | Lines |
|------|----------|-----------|-------|
| 2.1: Baseline | 1.0h | Setup/Review | 0 |
| 2.2: Stripe consolidation | 0.5h | Refactor | -15 |
| 2.3: InOutSparkTipX simplify | 0.5h | Refactor | -5 |
| 2.4: Base class verification | 0.5h | Review | 0 |
| 2.5: Parser optimization | 1.0h | Refactor | -30 |
| 2.6: Trivial type hints | 0.5h | Optimize | 0 |
| 2.7: Effect consolidation | 1.0h | Review | -5 |
| 2.8: Code cleanup | 0.5h | Polish | +10 |
| 2.9: Build & test | 1.0h | Validation | 0 |
| 2.10: Review & commit | 0.5h | Finalization | 0 |
| **TOTAL** | **7.0h** | | **-50 lines** |

**Target: 4250 → ~2400-2450 lines (43% reduction)**

---

## Success Criteria Checklist

**Before Phase 3:**
- [x] sd_style.h refactored, size reduced ~40%
- [x] No behavioral changes (all consolidations preserve functionality)
- [x] Build clean with zero compiler warnings
- [x] All example styles parse correctly
- [x] Frame rate within 5% of baseline
- [x] Code well-documented with clear comments
- [x] Ready for Phase 3 debugging work

---

## Risk Mitigation

| Risk | Mitigation |
|------|-----------|
| Consolidation breaks parsing | Test each token after consolidation |
| Frame rate regression | Baseline in 2.1, compare in 2.9, investigate if regression |
| Inline hints cause issues | Use only on proven hot paths, verify build |
| Stripe consolidation visual issue | Test both Stripes and HardStripes tokens |
| Parser changes subtle bugs | Keep changes mechanical (move code, add comments) |

---

## Next Steps After Phase 2

1. **Commit refactored code**
2. **Update STATE.md** — Mark Phase 2 complete
3. **Begin Phase 3** — Run `/gsd:plan-phase 3` for debugging

**Phase 3 will address:**
- Fast ignition regression investigation
- Pre-ignited color fix
- Missing effects parsing
- Return value validation

---

**Phase 2 Plan Complete**
Ready to execute refactoring tasks 2.1 through 2.10

Next: Begin Task 2.1 (Baseline & Review)

